# 10 FPS 高召回精筛结果

## 输入与标注

- 视频：`data/videos/飞书20260804-170744.mp4`
- 粗扫：10 FPS、置信度 `0.10`、原篮筐 ROI
- 精筛：30 FPS、候选窗口 `±1.5s`
- 人工标注候选：17 个
- 真实进球：`1.35s`、`38.00s`、`154.85s`、`267.20s`

## 本轮规则调整

- 允许高速穿筐但只有一个下方观测点的候选，修复低置信度漏检；
- 篮筐下方判定距离从 `rim_y + 18` 调整为 `rim_y + 10`；
- 穿越后的反弹变化阈值从 `10px` 调整为 `4px`；
- 横向离开阈值从 `3 × rim_width` 调整为 `2 × rim_width`；
- 事件一旦确认穿越但后续轨迹被判定为反弹或横向离开，不再继续用更晚的检测点配对，避免错误配对。

## 结果

```text
粗候选：17 个
精筛保留：4 个
保留时间：1.35s、38.00s、154.85s、267.20s
```

在当前这 17 个已人工标注候选上：

```text
Precision：100%
Recall：100%
```

该指标只代表当前视频的已标注候选集，不代表整段视频或其它场馆的最终准确率。
